-- RANK, AT THE TOP: the Apex Crystal's (data/characters/character_apex_crystal.lua). It stands above every
-- other ranked body whatever its health, so it cannot be hurt until every crystal beneath it is gone.
return {
    name = "Apex",
    description = "It can't be hurt while any other ranked body of its side stands.",
    ranked = true,
    rankTop = true,
}
