-- UP FROM BELOW: the Deep-Delver's Pick. The first blow its bearer lands after it surfaces from any
-- go-under -- a Delve, Through the Rock -- is a critical (Combat.forcesCrit). The go-under stamps
-- status_surfaced on a bearer of this; the landing spends it.
return {
    name = "Up From Below",
    description = "The first blow you land after surfacing from a Delve or Through the Rock is a critical.",
    critOnSurfacing = true,
}
