-- UNFED: a mouth that comes round sooner for every turn it found nothing to do. A flag (`starves`) read by
-- Combat.feedHunger at both ends of a turn: a WAIT feeds a stack of status_starving, an ACTION eats them.
-- Carried by the Chimera's lion and goat (utility_unfed_mouth); the serpent always acts and judges its own
-- hunger on its coil (ability_coil).
return {
    name = "Unfed",
    description = "A turn with nothing to do makes its next action come sooner.",
    starves = true,
}
