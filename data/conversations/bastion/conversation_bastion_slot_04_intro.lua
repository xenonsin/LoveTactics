-- Conversation authored inline (English); localization ids (`tag`) are stamped by
-- tools/extract_strings.lua and must not be hand-edited. See models/conversation.lua.
return {
    title = "The List",
    cast  = { "bastion", "character_rowan" },

    script = {
        { "bastion", "The list. Knights who set their shields down. It is long, and we read it aloud.", tag = 1 },
        { "character_rowan", "Every Oathkeeper shield in the order carries a name off that list. We read them so they are not forgotten.", tag = 2 },
        { "bastion", "We read them so the entries can be closed.", tag = 3 },
        { "bastion", "One of them is still walking about in our forms, calling himself a captain.", tag = 4 },
        { "character_rowan", "...Then we close it.", tag = 5 },
    },
}
