-- Per-level stat gains for a character growing as a ROGUE (greed's class): the skirmisher.
-- See data/growth/knight.lua for the shape.
return {
    speed = 1,
    damage = 2,
    stamina = 3,
    health = 5,
}
