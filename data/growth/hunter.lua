-- Per-level stat gains for a character growing as a HUNTER (gluttony's class): the ranged skirmisher.
-- See data/growth/knight.lua for the shape.
return {
    damage = 2,
    stamina = 4,
    speed = 1,
    health = 3,
}
