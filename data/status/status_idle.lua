-- IDLE: Idle Hands' badge (data/traits/trait_idle_hands.lua). Everything costs nothing while it lasts, and
-- it lasts until the bearer uses something.
return {
    name = "Idle",
    abbr = "Idle",
    description = "Idle: the next thing it uses costs nothing.",
    color = { 0.690, 0.780, 0.840 },
    duration = math.huge,
    hideDuration = true,
    costMultiplier = 0,
}
