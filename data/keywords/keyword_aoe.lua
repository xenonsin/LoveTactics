-- Glossary entry for the `aoe` ability keyword (docs/weapons.md). Definitions live here, beside the
-- statuses, so the item tooltip can explain a mechanic it names without the UI hard-coding prose.
return {
    name = "Area",
    description = "Covers a shape of tiles rather than a single body -- everything standing in it is caught.",
}
