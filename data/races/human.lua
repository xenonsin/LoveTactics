-- Human: the city's own, and the race almost every bodied thing in the folder is.
--
-- IT DECLARES NOTHING BUT ITS KIND, and that is the point of having it rather than leaving the field
-- absent. "This body has no race" and "nobody has labelled this body" have to be different states --
-- the same lesson `tier` learned when rung 0 was declared rather than left blank -- and a race that
-- says only "human, humanoid" is the first of those two said out loud.
--
-- No tags, no resist, no stat line: a human's per-tag mitigation comes off the coat it bought, which is
-- docs/bestiary.md's rule for every bodied thing that shops. The race axis exists to say what a body is
-- when that answer is not "one of us".
return {
    name = "Human",
    description = "The people of the city and the roads between them. They buy what they need.",
    kind = "humanoid",
}
